type MedicalInformation @model @auth(rules: [{allow: public}]) {
  id: ID!
  Dev_ID: String
  Med_Time: String
  Med_Red: String
  Med_Blue: String
  Med_Yellow: String
  Image_Name: String
  Results: String
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 